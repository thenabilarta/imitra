import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  storyContainerPlaceHolder: {
    display: 'flex',
    flexDirection: 'row',
  },
  storyPlaceHolder: {
    width: 80,
    height: 80,
    backgroundColor: '#bcbcbc',
    margin: 10,
    borderRadius: 50,
    overflow: 'hidden',
  },
});
